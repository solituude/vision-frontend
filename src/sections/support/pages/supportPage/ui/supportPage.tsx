import {usePageTitle} from "common/shared/lib/hooks";

export const SupportPage = () => {
    usePageTitle("Поддержка");
    return(
        <div>Страница поддержки</div>
    )
}

export default SupportPage;